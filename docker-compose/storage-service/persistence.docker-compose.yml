# Change ~/masterVolume to the location where the .multichain content should be saved on your machine 
version: "3"
services:
  minio:
    extends:
      file: storage-service/docker-compose.yml
      service: minio
    volumes:
      - /minioVolume:/data
